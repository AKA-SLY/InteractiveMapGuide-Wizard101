import React from "react";
import ReactDOM from "react-dom/client";
import Wizard101Preview from "./Wizard101Preview";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <Wizard101Preview />
  </React.StrictMode>
);
